{
    "name": "sechatapp",
    "version": "1.0.0",
    "scripts": {
        "start": "concurrently \"cd frontend && npm start\" \"cd server && npm run dev\" "
    },
    "devDependencies": {
        "concurrently": "^7.0.0"
    }
}